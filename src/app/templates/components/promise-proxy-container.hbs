{{#if proxy.isFulfilled}}
  {{yield}}
{{else}}
  {{#if proxy.isPending}}
    {{spin-spinner-block sizeClass="sm" class="centered"}}
  {{else if proxy.isRejected}}
    {{resource-load-error reason=proxy.reason}}
  {{/if}}
{{/if}}
